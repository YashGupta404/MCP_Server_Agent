# ---- .NET ----
bin/
obj/
*.user
*.suo
.vs/
[Dd]ebug/
[Rr]elease/
artifacts/

# ASP.NET dev certs / local secrets (never commit)
*.pfx
secrets.json
appsettings.*.local.json
# Admin helper contains the MCP API key — never commit
bff/mcp-admin.ps1

# ---- Node / extension tooling ----
node_modules/
npm-debug.log*
yarn-error.log*

# ---- OS / editor ----
.DS_Store
Thumbs.db
*.swp
*.log

# ---- Misc ----
*.tmp

# Postman collections (may contain staging tokens / secrets — never commit)
*.postman_collection.json
*.postman_environment.json
